# First Ruby Program - basic math functions and addition

# declare function named 'a' that accepts an integer and returns 1
def a(u) return 1 end

# driver code
a = 3
b = 2

# interprets a + b and prints output
puts(a + b)

# interprets a(b) so the value is printed
puts(a b)
